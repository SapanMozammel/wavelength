# PRD — Landing Page (Part 2) — SUPERSEDED

**Status:** `[⬜]` Superseded on 2026-08-22. No step in this PRD was ever started,
so no completed history is lost.

Replaced by [`10-landing-page`](../10-landing-page/prd.md), which keeps this
plan's direction verbatim — name → concept → visual, dark-first, `signal-*`
violet against `canvas-dark` with `pulse-*` marking anything live, Framer Motion
for entrances and CSS keyframes for the continuous pulse — and adds the sections
the `/plan` PRD format requires: component-type decisions, server/client island
boundaries, testing strategy, performance budget, and SEO.

**Bonus decision:** the **live login field in the hero** was chosen over the
API-health-reflecting rings. The rings still respond to `wakeStatus`, but as
support for [`09-cold-start-narration`](../09-cold-start-narration/prd.md) rather
than as the headline.

**Explicitly not doing, unchanged:** testimonials, FAQ accordion, pricing table,
logo cloud, newsletter capture, cookie banner, generic feature grid. The brief
rules most of these out by name; the rest would dilute a page whose whole
argument is that this product does less on purpose.

Start at [`.claude/plans/README.md`](../README.md).
